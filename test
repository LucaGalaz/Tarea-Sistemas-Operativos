#include <QApplication>
#include <QWidget>

int main(int argc, char *argv[]) {
    QApplication app(argc, argv);

    QWidget ventana;   // ventana básica
    ventana.resize(400, 300);
    ventana.setWindowTitle("Mi primera ventana en Qt");
    ventana.show();

    return app.exec();
}
